#include<iostream>
#include<vector>
using namespace std;
int main(){
    vector<int>v;
    int x;
    cout<<"enter size : ";
    cin>>x;
    for(int i=0;i<x;i++){
        int element;
        cin>>element;
        v.push_back(element);
    }
    for(int i=0;i<v.size();i++){
        if(v[i]<100){
            if((v[i])
        }
    }
    return 0;
}